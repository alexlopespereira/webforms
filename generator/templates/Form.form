{
  "key": "{{context['formkey']}}",
  "name": "This is a form template",
  "fields": [
    {% for field in context['fields']%}
      {
          "id": "{{field}}",
          "name": "{{field}}",
          "type": "text",
          "required": false,
          "placeholder": "empty"
      },
    {% endfor %}
      {
          "id": "comentario",
          "name": "comentario",
          "type": "text",
          "required": false,
          "placeholder": "empty"
      } 
  ]
}
